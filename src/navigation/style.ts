import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  iconContainer: {
    alignItems: 'center',
    justifyContent: 'center',
    width: 70,
    marginTop: 20,
  },
  icon: {
    width: 24,
    height: 24,
    marginBottom: 2,
  },
  label: {
    fontSize: 12,
    textAlign: 'center',
  },
});
